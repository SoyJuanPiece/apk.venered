pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'Venered'
apply from: file('../node_modules/@react-native/gradle-plugin/src/main/settings/settings-react-native.gradle')

include ':app'
