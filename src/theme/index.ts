import { colors, darkColors } from './colors';
import { spacing, padding, margin, gap } from './spacing';
import { typography, fontWeights } from './typography';

export { colors, darkColors };
export { spacing, padding, margin, gap };
export { typography, fontWeights };

export const theme = {
  colors,
  spacing,
  typography,
};
